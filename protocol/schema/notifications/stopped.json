{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "stopped",
  "description": "The debuggee has stopped (breakpoint, step complete, exception, etc.)",
  "params": {
    "type": "object",
    "properties": {
      "reason": { "$ref": "common.json#/definitions/StopReason" },
      "threadId": { "type": "integer" },
      "description": { "type": ["string", "null"] }
    },
    "required": ["reason", "threadId"]
  }
}
